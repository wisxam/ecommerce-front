import styles from './styles.module.css';

const { footerContainer } = styles;
const Footer = () => {
	return (
		<div className={footerContainer}>
			<span>2024 wesoo wasoom ecomm. All rights reserved.</span>
		</div>
	);
};

export default Footer;
